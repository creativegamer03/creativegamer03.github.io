document.addEventListener("keydown", (e) => {
    if (e.key === "F1") {
        window.location.href = "/";
    }
});

document.addEventListener("keydown", (e) => {
    if (e.key === "F2") {
        window.location.href = "/about.html";
    }
});